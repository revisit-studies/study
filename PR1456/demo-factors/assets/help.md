# Factors Demo Help

This study demonstrates ReVISit's factor configuration language. Factor values define experimental conditions, and recursive expressions such as `cross`, `zip`, `concat`, `repeat`, and `sample` combine or schedule them. The generated trial parameters show which condition was assigned to each component.
