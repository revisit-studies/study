
import {Text} from '@mantine/core';
import { StimulusParams } from '../../../store/types';

  // eslint-disable-next-line @typescript-eslint/no-explicit-any
export function Bar() {

  return (
    <Text>I am a plot</Text>
  );
}

export default Bar;